"""Research code kept out of the public API: trained heads, feature caches, evaluation harness.

Nothing here is imported by `jul` itself; it backs the `jul lab ...` commands and the tuning scripts.
"""
